package org.ikigaidigital;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class TimeDepositApplicationTest {

    @Test
    void contextLoads() {
        // This test checks if the Spring application context loads successfully -- Helps ensure that all beans are properly configured and there are no issues with the application setup.
        // Test passes if the application context loads without exceptions
    }
}